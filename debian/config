#!/bin/sh -e

. /usr/share/debconf/confmodule

db_input high cobbler/enable_web || true
db_go

db_get cobbler/enable_web
if [ "$RET" = "true" ]; then
    db_input high cobbler/config_tips || true
else
    db_input low cobbler/config_tips || true
fi
db_go
